{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "cd presentation && npm ci",
  "buildCommand": "cd presentation && npm run build",
  "outputDirectory": "presentation/dist",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
